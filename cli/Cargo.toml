[package]
name = "cli"
version = "0.1.0"
edition = "2021"
description = "A command line tool for Leagus."

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "4.5.0", features = ["derive", "color", "cargo"] }
